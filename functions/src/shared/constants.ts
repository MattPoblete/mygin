/**
 * functions/src/shared/constants.ts — Constantes server-side de checkout.
 */

/** Costo de despacho plano en CLP. Ajustable; el cliente nunca lo dicta. */
export const SHIPPING_FLAT_CLP = 3990;

/** Minutos que dura la reserva de stock antes de expirar. */
export const RESERVATION_MINUTES = 20;
